---
phase: 5
plan: 02
title: Timeline UI, subtitle editor, transition picker, audio track manager (wavesurfer.js), video preview (Vidstack Player), export button, Tab 3+4 wiring
wave: 2
depends_on: [05-01]
files_modified:
  - package.json
  - src/components/project/workflow-tabs.tsx
  - src/components/video/timeline.tsx
  - src/components/video/timeline-track.tsx
  - src/components/video/scene-clip.tsx
  - src/components/video/subtitle-editor.tsx
  - src/components/video/subtitle-preview.tsx
  - src/components/video/transition-picker.tsx
  - src/components/video/audio-track-manager.tsx
  - src/components/video/audio-waveform.tsx
  - src/components/video/audio-library-dialog.tsx
  - src/components/video/video-preview.tsx
  - src/components/video/export-button.tsx
  - src/components/video/export-progress.tsx
  - src/components/project/voice-tab.tsx
  - src/components/project/video-tab.tsx
  - src/hooks/use-export-job.ts
  - src/hooks/use-audio-tracks.ts
  - src/hooks/use-scene-settings.ts
  - src/lib/video/audio-library.ts
  - src/__tests__/timeline.test.tsx
  - src/__tests__/subtitle-editor.test.tsx
  - src/__tests__/export-button.test.tsx
requirements_addressed:
  - EDIT-01
  - EDIT-02
  - EDIT-03
  - EDIT-04
  - EDIT-05
gap_closure: false
---

<objective>
Build the complete video editing UI: a horizontal timeline with scene clips and audio tracks, a subtitle styling editor with live preview, a transition type picker, an audio track manager with wavesurfer.js waveform visualization, a 9:16 video preview canvas using Vidstack Player, an export button with real-time progress tracking, and wire everything into Tab 3 (Voice) and Tab 4 (Final Video) of the workflow.

Requirements fulfilled: EDIT-01 (subtitle UI), EDIT-02 (transition UI), EDIT-03 (audio track UI), EDIT-04 (video preview), EDIT-05 (export button)
Phase goal contribution: Complete user-facing video assembly experience.
Complexity: XL
</objective>

<tasks>
<task id="5-02-01" type="auto">
  <name>Install vidstack and wavesurfer.js dependencies</name>
  <files>
    - package.json
  </files>
  <read_first>
    - package.json
    - CLAUDE.md
  </read_first>
  <action>
    Run the following commands to install new dependencies:

    ```bash
    npm install vidstack wavesurfer.js
    ```

    Verify the packages are added to `package.json` dependencies. Do NOT install `fluent-ffmpeg` or `@ffmpeg/ffmpeg` — FFmpeg is spawn-only on the server side.
  </action>
  <acceptance_criteria>
    - `grep -q 'vidstack' package.json` exits 0
    - `grep -q 'wavesurfer.js' package.json` exits 0
    - `grep -qv 'fluent-ffmpeg' package.json` exits 0 (must NOT contain fluent-ffmpeg)
    - `npm ls vidstack` exits 0
    - `npm ls wavesurfer.js` exits 0
  </acceptance_criteria>
</task>

<task id="5-02-02" type="auto">
  <name>Create royalty-free audio library data</name>
  <files>
    - src/lib/video/audio-library.ts
  </files>
  <read_first>
    - src/lib/video/types.ts
  </read_first>
  <action>
    Create `src/lib/video/audio-library.ts` — a static list of royalty-free audio entries that users can pick from.

    ```typescript
    import type { AudioLibraryEntry } from "./types";

    /**
     * Built-in royalty-free audio library.
     * These are placeholder entries — in production, replace URLs with actual
     * royalty-free audio files hosted in Supabase Storage or a CDN.
     *
     * Categories:
     * BGM: lo-fi, upbeat, dramatic, calm, cinematic
     * SFX: whoosh, pop, ding, transition, notification
     */
    export const AUDIO_LIBRARY: AudioLibraryEntry[] = [
      // --- BGM ---
      {
        id: "bgm-lofi-01",
        name: "Lo-Fi Study Beats",
        type: "bgm",
        url: "/audio/library/bgm-lofi-01.mp3",
        duration: 60,
        category: "lo-fi",
      },
      {
        id: "bgm-upbeat-01",
        name: "Upbeat Energy",
        type: "bgm",
        url: "/audio/library/bgm-upbeat-01.mp3",
        duration: 45,
        category: "upbeat",
      },
      {
        id: "bgm-dramatic-01",
        name: "Dramatic Tension",
        type: "bgm",
        url: "/audio/library/bgm-dramatic-01.mp3",
        duration: 50,
        category: "dramatic",
      },
      {
        id: "bgm-calm-01",
        name: "Calm & Peaceful",
        type: "bgm",
        url: "/audio/library/bgm-calm-01.mp3",
        duration: 55,
        category: "calm",
      },
      {
        id: "bgm-cinematic-01",
        name: "Cinematic Opener",
        type: "bgm",
        url: "/audio/library/bgm-cinematic-01.mp3",
        duration: 30,
        category: "cinematic",
      },
      // --- SFX ---
      {
        id: "sfx-whoosh-01",
        name: "Whoosh",
        type: "sfx",
        url: "/audio/library/sfx-whoosh-01.mp3",
        duration: 1,
        category: "whoosh",
      },
      {
        id: "sfx-pop-01",
        name: "Pop",
        type: "sfx",
        url: "/audio/library/sfx-pop-01.mp3",
        duration: 0.5,
        category: "pop",
      },
      {
        id: "sfx-ding-01",
        name: "Ding Notification",
        type: "sfx",
        url: "/audio/library/sfx-ding-01.mp3",
        duration: 1.5,
        category: "ding",
      },
      {
        id: "sfx-transition-01",
        name: "Scene Transition",
        type: "sfx",
        url: "/audio/library/sfx-transition-01.mp3",
        duration: 1,
        category: "transition",
      },
      {
        id: "sfx-notification-01",
        name: "Notification Bell",
        type: "sfx",
        url: "/audio/library/sfx-notification-01.mp3",
        duration: 1,
        category: "notification",
      },
    ];

    /** Get library entries filtered by type */
    export function getAudioLibrary(type?: "bgm" | "sfx"): AudioLibraryEntry[] {
      if (!type) return AUDIO_LIBRARY;
      return AUDIO_LIBRARY.filter((entry) => entry.type === type);
    }

    /** Find a library entry by ID */
    export function findAudioById(id: string): AudioLibraryEntry | undefined {
      return AUDIO_LIBRARY.find((entry) => entry.id === id);
    }
    ```
  </action>
  <acceptance_criteria>
    - `ls src/lib/video/audio-library.ts` exits 0
    - `grep -q 'AUDIO_LIBRARY' src/lib/video/audio-library.ts` exits 0
    - `grep -q 'getAudioLibrary' src/lib/video/audio-library.ts` exits 0
    - `grep -q 'findAudioById' src/lib/video/audio-library.ts` exits 0
    - `grep -q 'bgm' src/lib/video/audio-library.ts` exits 0
    - `grep -q 'sfx' src/lib/video/audio-library.ts` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task id="5-02-03" type="auto">
  <name>Create custom hooks for scene settings, audio tracks, and export job</name>
  <files>
    - src/hooks/use-scene-settings.ts
    - src/hooks/use-audio-tracks.ts
    - src/hooks/use-export-job.ts
  </files>
  <read_first>
    - src/lib/video/types.ts
    - src/lib/db/schema.ts
  </read_first>
  <action>
    **1. Create `src/hooks/use-scene-settings.ts`:**

    Custom hook for managing per-scene subtitle and transition settings.

    ```typescript
    "use client";

    import { useState, useCallback } from "react";
    import type { SubtitleStyle, TransitionType } from "@/lib/video/types";
    import { DEFAULT_SUBTITLE_STYLE } from "@/lib/video/types";

    interface SceneSettings {
      subtitleStyle: SubtitleStyle;
      transitionType: TransitionType;
      transitionDuration: number;
    }

    export function useSceneSettings(projectId: string, sceneId: string) {
      const [settings, setSettings] = useState<SceneSettings>({
        subtitleStyle: DEFAULT_SUBTITLE_STYLE,
        transitionType: "cut",
        transitionDuration: 0.5,
      });
      const [loading, setLoading] = useState(false);
      const [error, setError] = useState<string | null>(null);

      /** Fetch current subtitle/transition settings from API */
      const fetchSettings = useCallback(async () => {
        setLoading(true);
        try {
          const [subRes, transRes] = await Promise.all([
            fetch(`/api/projects/${projectId}/scenes/${sceneId}/subtitle`),
            fetch(`/api/projects/${projectId}/scenes/${sceneId}/transition`),
          ]);
          if (subRes.ok) {
            const subData = await subRes.json();
            setSettings((prev) => ({
              ...prev,
              subtitleStyle: subData.subtitleStyle || DEFAULT_SUBTITLE_STYLE,
            }));
          }
          if (transRes.ok) {
            const transData = await transRes.json();
            setSettings((prev) => ({
              ...prev,
              transitionType: transData.transitionType || "cut",
              transitionDuration: transData.transitionDuration || 0.5,
            }));
          }
        } catch (err) {
          setError(err instanceof Error ? err.message : "Failed to load settings");
        } finally {
          setLoading(false);
        }
      }, [projectId, sceneId]);

      /** Update subtitle style */
      const updateSubtitle = useCallback(async (style: SubtitleStyle) => {
        setLoading(true);
        try {
          const res = await fetch(`/api/projects/${projectId}/scenes/${sceneId}/subtitle`, {
            method: "PUT",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify(style),
          });
          if (!res.ok) throw new Error("Failed to update subtitle");
          setSettings((prev) => ({ ...prev, subtitleStyle: style }));
        } catch (err) {
          setError(err instanceof Error ? err.message : "Update failed");
        } finally {
          setLoading(false);
        }
      }, [projectId, sceneId]);

      /** Update transition */
      const updateTransition = useCallback(async (type: TransitionType, duration: number) => {
        setLoading(true);
        try {
          const res = await fetch(`/api/projects/${projectId}/scenes/${sceneId}/transition`, {
            method: "PUT",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ transitionType: type, transitionDuration: duration }),
          });
          if (!res.ok) throw new Error("Failed to update transition");
          setSettings((prev) => ({ ...prev, transitionType: type, transitionDuration: duration }));
        } catch (err) {
          setError(err instanceof Error ? err.message : "Update failed");
        } finally {
          setLoading(false);
        }
      }, [projectId, sceneId]);

      return { settings, loading, error, fetchSettings, updateSubtitle, updateTransition };
    }
    ```

    **2. Create `src/hooks/use-audio-tracks.ts`:**

    Custom hook for managing audio tracks for a project.
    - `tracks` state array.
    - `fetchTracks()` — GET `/api/projects/{id}/audio-tracks`.
    - `addTrack(data)` — POST to add from library or upload.
    - `updateTrack(trackId, data)` — PUT to adjust timing/volume.
    - `removeTrack(trackId)` — DELETE.
    - Loading and error states.

    **3. Create `src/hooks/use-export-job.ts`:**

    Custom hook for triggering and tracking export jobs.
    - `exportStatus` state: `'idle' | 'pending' | 'rendering' | 'uploading' | 'complete' | 'failed'`.
    - `progress` state (0-100).
    - `exportedUrl` state.
    - `startExport()` — POST to `/api/projects/{id}/export`, then poll GET `/api/projects/{id}/export` every 2 seconds until complete.
    - `resetExport()` — reset to idle state.
    - Uses `useEffect` cleanup to clear the polling interval.
  </action>
  <acceptance_criteria>
    - `ls src/hooks/use-scene-settings.ts` exits 0
    - `ls src/hooks/use-audio-tracks.ts` exits 0
    - `ls src/hooks/use-export-job.ts` exits 0
    - `grep -q 'useSceneSettings' src/hooks/use-scene-settings.ts` exits 0
    - `grep -q 'useAudioTracks' src/hooks/use-audio-tracks.ts` exits 0
    - `grep -q 'useExportJob' src/hooks/use-export-job.ts` exits 0
    - `grep -q 'DEFAULT_SUBTITLE_STYLE' src/hooks/use-scene-settings.ts` exits 0
    - `grep -q 'startExport' src/hooks/use-export-job.ts` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task id="5-02-04" type="auto">
  <name>Build timeline UI components (timeline, track, scene clip)</name>
  <files>
    - src/components/video/timeline.tsx
    - src/components/video/timeline-track.tsx
    - src/components/video/scene-clip.tsx
  </files>
  <read_first>
    - src/lib/video/types.ts
    - src/components/project/workflow-tabs.tsx
  </read_first>
  <action>
    **1. Create `src/components/video/timeline.tsx`:**

    A horizontal scrollable timeline that shows all scenes as clips on a video track and audio tracks below.

    ```typescript
    "use client";

    interface TimelineProps {
      scenes: Array<{
        id: string;
        sceneIndex: number;
        narration: string;
        duration: number;
        thumbnailUrl?: string;
        transitionType: string;
      }>;
      audioTracks: Array<{
        id: string;
        name: string;
        type: "bgm" | "sfx";
        startTime: number;
        endTime: number | null;
        volume: number;
      }>;
      totalDuration: number;
      selectedSceneId: string | null;
      onSceneSelect: (sceneId: string) => void;
      onSceneReorder?: (fromIndex: number, toIndex: number) => void;
      pixelsPerSecond?: number; // default 40
    }
    ```

    Implementation:
    - Container: horizontally scrollable `div` with `overflow-x-auto`.
    - Time ruler: top bar showing tick marks every second, labels every 5 seconds.
    - Video track row: scenes arranged horizontally, each as a `SceneClip` component. Width = `duration * pixelsPerSecond`. Between clips, show a small transition icon when transitionType !== 'cut'.
    - Audio track rows: below the video track. Each audio track is a `TimelineTrack` rendered as a horizontal bar positioned at `startTime` with width = `(endTime - startTime) * pixelsPerSecond`.
    - Current time indicator: a red vertical line that can be dragged.
    - Total width = `totalDuration * pixelsPerSecond`.

    **2. Create `src/components/video/timeline-track.tsx`:**

    A single horizontal track row (used for audio tracks).
    - Renders a colored bar at the correct horizontal position.
    - Shows track name and volume indicator.
    - Click to select.
    - BGM tracks in blue, SFX tracks in green.

    **3. Create `src/components/video/scene-clip.tsx`:**

    A single scene clip in the timeline.
    - Shows a thumbnail (scaled down scene image) or a colored placeholder.
    - Shows scene index number.
    - Shows narration text (truncated).
    - Duration label.
    - Transition badge (small icon between clips): fade icon for fade, arrows for slide, etc.
    - Highlighted border when selected.
    - Click to select the scene (triggers `onSceneSelect`).
  </action>
  <acceptance_criteria>
    - `ls src/components/video/timeline.tsx` exits 0
    - `ls src/components/video/timeline-track.tsx` exits 0
    - `ls src/components/video/scene-clip.tsx` exits 0
    - `grep -q 'Timeline' src/components/video/timeline.tsx` exits 0
    - `grep -q 'TimelineTrack' src/components/video/timeline-track.tsx` exits 0
    - `grep -q 'SceneClip' src/components/video/scene-clip.tsx` exits 0
    - `grep -q 'pixelsPerSecond' src/components/video/timeline.tsx` exits 0
    - `grep -q 'onSceneSelect' src/components/video/timeline.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task id="5-02-05" type="auto">
  <name>Build subtitle editor component with live preview</name>
  <files>
    - src/components/video/subtitle-editor.tsx
    - src/components/video/subtitle-preview.tsx
    - src/__tests__/subtitle-editor.test.tsx
  </files>
  <read_first>
    - src/lib/video/types.ts
    - src/hooks/use-scene-settings.ts
  </read_first>
  <action>
    **1. Create `src/components/video/subtitle-editor.tsx`:**

    A panel for editing subtitle styling on the selected scene.

    Props:
    ```typescript
    interface SubtitleEditorProps {
      sceneId: string;
      narrationText: string;
      style: SubtitleStyle;
      onStyleChange: (style: SubtitleStyle) => void;
    }
    ```

    UI controls:
    - **Font family**: dropdown select with options: 'Noto Sans KR', 'Pretendard', 'Nanum Gothic', 'Nanum Myeongjo', 'Black Han Sans'. Use shadcn `Select` component.
    - **Font size**: number input with slider (range 16-72, step 2). Use shadcn `Slider` + `Input`.
    - **Font color**: color input (hex). Use `<input type="color">` with hex text input.
    - **Background color**: color input with opacity slider (0-100%). Combine hex color + alpha to produce hex with alpha.
    - **Border color**: color input.
    - **Border width**: slider (0-4, step 1).
    - **Shadow color**: color input.
    - **Shadow offset**: slider (0-4, step 1).
    - **Position**: 3-button toggle group: top / center / bottom. Use shadcn `ToggleGroup`.
    - **Reset button**: resets to `DEFAULT_SUBTITLE_STYLE`.
    - All changes call `onStyleChange` immediately (debounced by 300ms to avoid excessive API calls).

    **2. Create `src/components/video/subtitle-preview.tsx`:**

    A small 9:16 preview box (e.g., 270x480 scaled) showing what the subtitle will look like.
    - Renders the narration text with the current SubtitleStyle applied as inline CSS.
    - Position maps to CSS: top → `top: 10%`, center → `top: 50%; transform: translateY(-50%)`, bottom → `bottom: 15%`.
    - Background color applied as `backgroundColor` on the text span.
    - Border and shadow applied via CSS `textShadow` and `webkitTextStroke`.
    - Dark background to simulate video behind the text.

    **3. Create `src/__tests__/subtitle-editor.test.tsx`:**

    ```typescript
    import { describe, it, expect, vi } from "vitest";
    import { render, screen, fireEvent } from "@testing-library/react";
    import { SubtitleEditor } from "@/components/video/subtitle-editor";
    import { DEFAULT_SUBTITLE_STYLE } from "@/lib/video/types";

    describe("SubtitleEditor", () => {
      it("renders all style controls", () => {
        render(
          <SubtitleEditor
            sceneId="test-scene"
            narrationText="테스트 자막"
            style={DEFAULT_SUBTITLE_STYLE}
            onStyleChange={vi.fn()}
          />
        );
        expect(screen.getByText(/폰트/i)).toBeInTheDocument();
        expect(screen.getByText(/크기/i)).toBeInTheDocument();
        expect(screen.getByText(/위치/i)).toBeInTheDocument();
      });

      it("calls onStyleChange when position changes", async () => {
        const onChange = vi.fn();
        render(
          <SubtitleEditor
            sceneId="test-scene"
            narrationText="테스트"
            style={DEFAULT_SUBTITLE_STYLE}
            onStyleChange={onChange}
          />
        );
        // Click the "top" position button
        const topBtn = screen.getByRole("radio", { name: /top|상단/i });
        fireEvent.click(topBtn);
        // onChange should be called (may be debounced)
      });
    });
    ```

    Note: Install `@testing-library/react` and `@testing-library/jest-dom` as devDependencies if not already present.
  </action>
  <acceptance_criteria>
    - `ls src/components/video/subtitle-editor.tsx` exits 0
    - `ls src/components/video/subtitle-preview.tsx` exits 0
    - `ls src/__tests__/subtitle-editor.test.tsx` exits 0
    - `grep -q 'SubtitleEditor' src/components/video/subtitle-editor.tsx` exits 0
    - `grep -q 'SubtitlePreview' src/components/video/subtitle-preview.tsx` exits 0
    - `grep -q 'fontFamily' src/components/video/subtitle-editor.tsx` exits 0
    - `grep -q 'fontSize' src/components/video/subtitle-editor.tsx` exits 0
    - `grep -q 'position' src/components/video/subtitle-editor.tsx` exits 0
    - `grep -q 'DEFAULT_SUBTITLE_STYLE' src/components/video/subtitle-editor.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task id="5-02-06" type="auto">
  <name>Build transition picker component</name>
  <files>
    - src/components/video/transition-picker.tsx
  </files>
  <read_first>
    - src/lib/video/types.ts
    - src/hooks/use-scene-settings.ts
  </read_first>
  <action>
    Create `src/components/video/transition-picker.tsx`:

    Props:
    ```typescript
    interface TransitionPickerProps {
      sceneId: string;
      currentType: TransitionType;
      currentDuration: number;
      onTransitionChange: (type: TransitionType, duration: number) => void;
    }
    ```

    UI:
    - A grid of 6 transition type cards (2 columns, 3 rows), each showing:
      - Icon representation: fade (gradient icon), dissolve (overlapping squares), slide-left (left arrow), slide-right (right arrow), zoom-in (zoom icon), cut (scissors icon). Use Lucide icons.
      - Label text (한국어): 페이드, 디졸브, 왼쪽 슬라이드, 오른쪽 슬라이드, 줌 인, 컷.
      - Highlighted border/background when selected.
    - Below the grid: **Duration slider** (0.2s - 1.0s, step 0.1) — only visible when type is NOT 'cut'.
    - Duration label showing current value (e.g., "0.5초").
    - Changes call `onTransitionChange` immediately.
  </action>
  <acceptance_criteria>
    - `ls src/components/video/transition-picker.tsx` exits 0
    - `grep -q 'TransitionPicker' src/components/video/transition-picker.tsx` exits 0
    - `grep -q 'fade' src/components/video/transition-picker.tsx` exits 0
    - `grep -q 'dissolve' src/components/video/transition-picker.tsx` exits 0
    - `grep -q 'slide-left' src/components/video/transition-picker.tsx` exits 0
    - `grep -q 'zoom-in' src/components/video/transition-picker.tsx` exits 0
    - `grep -q 'cut' src/components/video/transition-picker.tsx` exits 0
    - `grep -q 'onTransitionChange' src/components/video/transition-picker.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task id="5-02-07" type="auto">
  <name>Build audio track manager with wavesurfer.js waveform</name>
  <files>
    - src/components/video/audio-track-manager.tsx
    - src/components/video/audio-waveform.tsx
    - src/components/video/audio-library-dialog.tsx
  </files>
  <read_first>
    - src/lib/video/types.ts
    - src/lib/video/audio-library.ts
    - src/hooks/use-audio-tracks.ts
    - package.json
  </read_first>
  <action>
    **1. Create `src/components/video/audio-track-manager.tsx`:**

    A panel for managing BGM and SFX audio tracks on the project.

    Props:
    ```typescript
    interface AudioTrackManagerProps {
      projectId: string;
      tracks: Array<{
        id: string;
        name: string;
        type: "bgm" | "sfx";
        url: string;
        startTime: number;
        endTime: number | null;
        volume: number;
      }>;
      onAddTrack: (data: { type: "bgm" | "sfx"; name: string; libraryId?: string; file?: File }) => void;
      onUpdateTrack: (trackId: string, data: { startTime?: number; endTime?: number | null; volume?: number }) => void;
      onRemoveTrack: (trackId: string) => void;
    }
    ```

    UI:
    - Section header: "배경음악 & 효과음"
    - **Add buttons**: "BGM 추가" and "효과음 추가" — opens the `AudioLibraryDialog`.
    - **Track list**: each track shows:
      - Name and type badge (BGM = blue, SFX = green).
      - `AudioWaveform` component showing the waveform.
      - Volume slider (0-100%).
      - Start time input (seconds).
      - End time input (seconds, optional).
      - Delete button (trash icon).
    - Upload button: "직접 업로드" — file input accepting .mp3, .wav, .ogg.

    **2. Create `src/components/video/audio-waveform.tsx`:**

    A waveform visualization component using wavesurfer.js.

    Props:
    ```typescript
    interface AudioWaveformProps {
      audioUrl: string;
      height?: number; // default 48
      waveColor?: string; // default '#8884d8'
      progressColor?: string; // default '#6366f1'
      onReady?: (duration: number) => void;
    }
    ```

    Implementation:
    - Use `useRef` for the container div and wavesurfer instance.
    - Initialize wavesurfer in a `useEffect`:
      ```typescript
      import WaveSurfer from "wavesurfer.js";

      useEffect(() => {
        if (!containerRef.current) return;
        const ws = WaveSurfer.create({
          container: containerRef.current,
          waveColor: waveColor,
          progressColor: progressColor,
          height: height,
          barWidth: 2,
          barGap: 1,
          barRadius: 2,
          cursorWidth: 0,
          interact: false,
        });
        ws.load(audioUrl);
        ws.on("ready", () => onReady?.(ws.getDuration()));
        wavesurferRef.current = ws;
        return () => ws.destroy();
      }, [audioUrl]);
      ```
    - Render as a simple div container.

    **3. Create `src/components/video/audio-library-dialog.tsx`:**

    A dialog/modal for browsing and selecting from the built-in audio library.

    Props:
    ```typescript
    interface AudioLibraryDialogProps {
      open: boolean;
      onClose: () => void;
      type: "bgm" | "sfx";
      onSelect: (entry: AudioLibraryEntry) => void;
    }
    ```

    UI:
    - Use shadcn `Dialog` component.
    - Title: "BGM 라이브러리" or "효과음 라이브러리" based on type.
    - Grid of audio library entries (from `getAudioLibrary(type)`).
    - Each entry shows: name, category badge, duration.
    - Play preview button (small audio player using `<audio>` element).
    - "선택" (Select) button per entry — calls `onSelect` and closes dialog.
  </action>
  <acceptance_criteria>
    - `ls src/components/video/audio-track-manager.tsx` exits 0
    - `ls src/components/video/audio-waveform.tsx` exits 0
    - `ls src/components/video/audio-library-dialog.tsx` exits 0
    - `grep -q 'AudioTrackManager' src/components/video/audio-track-manager.tsx` exits 0
    - `grep -q 'AudioWaveform' src/components/video/audio-waveform.tsx` exits 0
    - `grep -q 'AudioLibraryDialog' src/components/video/audio-library-dialog.tsx` exits 0
    - `grep -q 'WaveSurfer' src/components/video/audio-waveform.tsx` exits 0
    - `grep -q 'wavesurfer.js' src/components/video/audio-waveform.tsx` exits 0
    - `grep -q 'volume' src/components/video/audio-track-manager.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task id="5-02-08" type="auto">
  <name>Build video preview component with Vidstack Player</name>
  <files>
    - src/components/video/video-preview.tsx
  </files>
  <read_first>
    - src/lib/video/types.ts
    - package.json
  </read_first>
  <action>
    Create `src/components/video/video-preview.tsx`:

    A 9:16 video preview canvas using Vidstack Player for scene-by-scene playback.

    Props:
    ```typescript
    interface VideoPreviewProps {
      scenes: Array<{
        id: string;
        sceneIndex: number;
        narration: string;
        duration: number;
        mediaUrl: string;
        mediaType: "image" | "video";
        audioUrl?: string;
        subtitleStyle: SubtitleStyle | null;
      }>;
      exportedVideoUrl?: string | null;
      onTimeUpdate?: (currentTime: number) => void;
    }
    ```

    Implementation:
    - **If `exportedVideoUrl` is provided**: render a single Vidstack Player with the exported MP4 URL.
      ```tsx
      import { MediaPlayer, MediaProvider } from "@vidstack/react";

      <MediaPlayer
        src={exportedVideoUrl}
        aspectRatio="9/16"
        className="w-full max-w-[360px] mx-auto"
      >
        <MediaProvider />
      </MediaPlayer>
      ```
    - **If no export**: render a scene-by-scene preview:
      - Show the current scene's media (image or video) in a 9:16 container.
      - Overlay the subtitle text with the scene's `subtitleStyle` applied as CSS (same as `SubtitlePreview`).
      - Play/pause button.
      - Scene navigation: prev/next buttons.
      - If scene has an `audioUrl` (TTS), play it synchronized with the scene display.
      - Auto-advance to next scene when current scene duration expires.
      - Use `useState` for currentSceneIndex and playing state.
      - Use `useEffect` with `setTimeout` for auto-advance timing.
    - Container: centered, max-width 360px, 9:16 aspect ratio, dark background, rounded corners.
    - Controls below: play/pause, scene indicator (e.g., "3/8"), prev/next.
  </action>
  <acceptance_criteria>
    - `ls src/components/video/video-preview.tsx` exits 0
    - `grep -q 'VideoPreview' src/components/video/video-preview.tsx` exits 0
    - `grep -q 'MediaPlayer' src/components/video/video-preview.tsx` exits 0
    - `grep -q 'vidstack' src/components/video/video-preview.tsx` exits 0
    - `grep -q '9/16' src/components/video/video-preview.tsx` exits 0
    - `grep -q 'exportedVideoUrl' src/components/video/video-preview.tsx` exits 0
    - `grep -q 'subtitleStyle' src/components/video/video-preview.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task id="5-02-09" type="auto">
  <name>Build export button and progress components</name>
  <files>
    - src/components/video/export-button.tsx
    - src/components/video/export-progress.tsx
    - src/__tests__/export-button.test.tsx
  </files>
  <read_first>
    - src/hooks/use-export-job.ts
    - src/lib/video/types.ts
  </read_first>
  <action>
    **1. Create `src/components/video/export-button.tsx`:**

    Props:
    ```typescript
    interface ExportButtonProps {
      projectId: string;
      disabled?: boolean;
      onExportComplete?: (url: string) => void;
    }
    ```

    Implementation:
    - Uses the `useExportJob` hook.
    - Button states:
      - **Idle**: "MP4 내보내기" with download icon. Click calls `startExport()`.
      - **Pending/Rendering/Uploading**: shows `ExportProgress` component instead of button.
      - **Complete**: "다운로드" button with the exported URL + "다시 내보내기" secondary button.
      - **Failed**: "내보내기 실패" error message + "다시 시도" retry button.
    - When complete, calls `onExportComplete(url)`.

    **2. Create `src/components/video/export-progress.tsx`:**

    Props:
    ```typescript
    interface ExportProgressProps {
      status: string;
      progress: number;
      currentStep?: string;
    }
    ```

    UI:
    - shadcn `Progress` bar component showing 0-100%.
    - Status label: "대기 중...", "영상 렌더링 중... (45%)", "업로드 중...", "완료!".
    - Estimated time remaining (rough calculation based on progress rate).
    - Cancel button (optional, for future implementation).

    **3. Create `src/__tests__/export-button.test.tsx`:**

    ```typescript
    import { describe, it, expect, vi } from "vitest";
    import { render, screen } from "@testing-library/react";
    import { ExportButton } from "@/components/video/export-button";

    // Mock the useExportJob hook
    vi.mock("@/hooks/use-export-job", () => ({
      useExportJob: () => ({
        status: "idle",
        progress: 0,
        exportedUrl: null,
        startExport: vi.fn(),
        resetExport: vi.fn(),
      }),
    }));

    describe("ExportButton", () => {
      it("renders idle state with export button", () => {
        render(<ExportButton projectId="test-id" />);
        expect(screen.getByText(/내보내기/)).toBeInTheDocument();
      });

      it("disables button when disabled prop is true", () => {
        render(<ExportButton projectId="test-id" disabled />);
        const button = screen.getByRole("button");
        expect(button).toBeDisabled();
      });
    });
    ```
  </action>
  <acceptance_criteria>
    - `ls src/components/video/export-button.tsx` exits 0
    - `ls src/components/video/export-progress.tsx` exits 0
    - `ls src/__tests__/export-button.test.tsx` exits 0
    - `grep -q 'ExportButton' src/components/video/export-button.tsx` exits 0
    - `grep -q 'ExportProgress' src/components/video/export-progress.tsx` exits 0
    - `grep -q 'useExportJob' src/components/video/export-button.tsx` exits 0
    - `grep -q 'startExport' src/components/video/export-button.tsx` exits 0
    - `grep -q 'Progress' src/components/video/export-progress.tsx` exits 0
    - `npx vitest run src/__tests__/export-button.test.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task id="5-02-10" type="auto">
  <name>Build Voice tab (Tab 3) content component</name>
  <files>
    - src/components/project/voice-tab.tsx
  </files>
  <read_first>
    - src/components/project/workflow-tabs.tsx
    - src/lib/video/types.ts
    - src/hooks/use-audio-tracks.ts
  </read_first>
  <action>
    Create `src/components/project/voice-tab.tsx`:

    This component serves as the content for Tab 3 ("음성") in the workflow. It combines TTS voice controls (already built in Phase 4 — reference existing voice profile and TTS generation components) with the new audio track management.

    Props:
    ```typescript
    interface VoiceTabProps {
      projectId: string;
      scriptId: string;
      scenes: Array<{
        id: string;
        sceneIndex: number;
        narration: string;
        audioUrl?: string;
      }>;
    }
    ```

    Layout (vertical stack):
    1. **TTS Section** (top half):
       - Header: "나레이션 음성"
       - For each scene: show narration text, TTS status (generated/pending), play button, regenerate button.
       - Voice profile selector (link to existing voice profile management).
       - "전체 TTS 생성" button to generate TTS for all scenes.
    2. **Audio Track Section** (bottom half):
       - Render `AudioTrackManager` component.
       - Pass `useAudioTracks` hook data.
    3. **Next Step**: "다음: 최종 영상" button that advances to Tab 4 (calls the tab change handler from parent).
  </action>
  <acceptance_criteria>
    - `ls src/components/project/voice-tab.tsx` exits 0
    - `grep -q 'VoiceTab' src/components/project/voice-tab.tsx` exits 0
    - `grep -q 'AudioTrackManager' src/components/project/voice-tab.tsx` exits 0
    - `grep -q 'projectId' src/components/project/voice-tab.tsx` exits 0
    - `grep -q 'TTS' src/components/project/voice-tab.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task id="5-02-11" type="auto">
  <name>Build Video tab (Tab 4) content component — full video assembly UI</name>
  <files>
    - src/components/project/video-tab.tsx
  </files>
  <read_first>
    - src/components/project/workflow-tabs.tsx
    - src/components/video/timeline.tsx
    - src/components/video/subtitle-editor.tsx
    - src/components/video/transition-picker.tsx
    - src/components/video/video-preview.tsx
    - src/components/video/export-button.tsx
    - src/hooks/use-scene-settings.ts
    - src/hooks/use-export-job.ts
  </read_first>
  <action>
    Create `src/components/project/video-tab.tsx`:

    This is the main content for Tab 4 ("최종 영상") — the full video assembly workspace.

    Props:
    ```typescript
    interface VideoTabProps {
      projectId: string;
      scriptId: string;
    }
    ```

    Layout (responsive, 2-column on desktop):
    ```
    +------------------------------------------+
    |  [Video Preview]  |  [Edit Panel]        |
    |   9:16 canvas     |  - Subtitle Editor   |
    |   with controls   |  - Transition Picker |
    |                   |                      |
    +------------------------------------------+
    |  [Timeline]                              |
    |  Scene clips + audio tracks              |
    +------------------------------------------+
    |  [Export Button]                          |
    +------------------------------------------+
    ```

    Implementation:
    - Fetch scenes for the scriptId on mount (GET `/api/projects/{projectId}/scenes`).
    - Fetch media assets for each scene.
    - Use `useState` for `selectedSceneId`.
    - When a scene is selected (from timeline click), show its subtitle editor and transition picker in the right panel.
    - Use `useSceneSettings` hook for the selected scene.
    - Render `VideoPreview` with all scenes data.
    - Render `Timeline` with scenes and audio tracks.
    - Render `ExportButton` at the bottom.
    - When export completes, update `VideoPreview` to show the exported video.
    - Empty state: if no scenes exist, show a message "먼저 대본을 장면으로 분할해주세요" with a link back to Tab 2.
  </action>
  <acceptance_criteria>
    - `ls src/components/project/video-tab.tsx` exits 0
    - `grep -q 'VideoTab' src/components/project/video-tab.tsx` exits 0
    - `grep -q 'VideoPreview' src/components/project/video-tab.tsx` exits 0
    - `grep -q 'Timeline' src/components/project/video-tab.tsx` exits 0
    - `grep -q 'SubtitleEditor' src/components/project/video-tab.tsx` exits 0
    - `grep -q 'TransitionPicker' src/components/project/video-tab.tsx` exits 0
    - `grep -q 'ExportButton' src/components/project/video-tab.tsx` exits 0
    - `grep -q 'selectedSceneId' src/components/project/video-tab.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task id="5-02-12" type="auto">
  <name>Wire Tab 3 and Tab 4 into workflow-tabs (enable all 4 tabs)</name>
  <files>
    - src/components/project/workflow-tabs.tsx
  </files>
  <read_first>
    - src/components/project/workflow-tabs.tsx
    - src/components/project/voice-tab.tsx
    - src/components/project/video-tab.tsx
  </read_first>
  <action>
    Update `src/components/project/workflow-tabs.tsx`:

    1. **Remove the disabled state for tabs 3 and 4.** Change:
       ```typescript
       const isDisabled = tab.step > 2; // Tabs 1-2 active in Phase 4
       ```
       to:
       ```typescript
       const isDisabled = false; // All tabs active in Phase 5
       ```

    2. The `children` prop already accepts `voice` and `video` ReactNode slots, so no interface change is needed. The parent page component (wherever `WorkflowTabs` is rendered) must now pass `VoiceTab` and `VideoTab` as the children:

       ```tsx
       <WorkflowTabs
         activeTab={activeTab}
         onTabChange={setActiveTab}
         completedSteps={completedSteps}
       >
         {{
           script: <ScriptTabContent ... />,
           scene: <SceneTab ... />,
           voice: <VoiceTab projectId={projectId} scriptId={scriptId} scenes={scenes} />,
           video: <VideoTab projectId={projectId} scriptId={scriptId} />,
         }}
       </WorkflowTabs>
       ```

    3. Verify that the parent page component (find it via grep for `<WorkflowTabs`) is updated to pass `VoiceTab` and `VideoTab` instead of placeholder content. If the parent currently passes placeholder divs for `voice` and `video`, replace them with the actual components.
  </action>
  <acceptance_criteria>
    - `grep -q 'false' src/components/project/workflow-tabs.tsx` exits 0 (isDisabled = false)
    - `grep -qv 'tab.step > 2' src/components/project/workflow-tabs.tsx` exits 0 (old disabled logic removed)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task id="5-02-13" type="auto">
  <name>Create timeline component test</name>
  <files>
    - src/__tests__/timeline.test.tsx
  </files>
  <read_first>
    - src/components/video/timeline.tsx
    - src/components/video/scene-clip.tsx
  </read_first>
  <action>
    Create `src/__tests__/timeline.test.tsx`:

    ```typescript
    import { describe, it, expect, vi } from "vitest";
    import { render, screen, fireEvent } from "@testing-library/react";
    import { Timeline } from "@/components/video/timeline";

    describe("Timeline", () => {
      const mockScenes = [
        { id: "s1", sceneIndex: 0, narration: "첫 번째 장면", duration: 3, transitionType: "cut" },
        { id: "s2", sceneIndex: 1, narration: "두 번째 장면", duration: 4, transitionType: "fade" },
        { id: "s3", sceneIndex: 2, narration: "세 번째 장면", duration: 3, transitionType: "cut" },
      ];

      it("renders all scene clips", () => {
        render(
          <Timeline
            scenes={mockScenes}
            audioTracks={[]}
            totalDuration={10}
            selectedSceneId={null}
            onSceneSelect={vi.fn()}
          />
        );
        expect(screen.getByText(/첫 번째/)).toBeInTheDocument();
        expect(screen.getByText(/두 번째/)).toBeInTheDocument();
        expect(screen.getByText(/세 번째/)).toBeInTheDocument();
      });

      it("calls onSceneSelect when clicking a scene", () => {
        const onSelect = vi.fn();
        render(
          <Timeline
            scenes={mockScenes}
            audioTracks={[]}
            totalDuration={10}
            selectedSceneId={null}
            onSceneSelect={onSelect}
          />
        );
        fireEvent.click(screen.getByText(/첫 번째/));
        expect(onSelect).toHaveBeenCalledWith("s1");
      });

      it("highlights selected scene", () => {
        render(
          <Timeline
            scenes={mockScenes}
            audioTracks={[]}
            totalDuration={10}
            selectedSceneId="s2"
            onSceneSelect={vi.fn()}
          />
        );
        // The selected scene should have a visual indicator (check class or attribute)
      });
    });
    ```

    Note: If `@testing-library/react` and `@testing-library/jest-dom` are not yet installed, add them:
    ```bash
    npm install -D @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom
    ```
    And ensure vitest config includes `environment: 'jsdom'` for tsx tests.
  </action>
  <acceptance_criteria>
    - `ls src/__tests__/timeline.test.tsx` exits 0
    - `grep -q 'Timeline' src/__tests__/timeline.test.tsx` exits 0
    - `grep -q 'onSceneSelect' src/__tests__/timeline.test.tsx` exits 0
    - `npx vitest run src/__tests__/timeline.test.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>
</tasks>

<done_when>
  - Dependencies: vidstack and wavesurfer.js are installed (in package.json)
  - Timeline: horizontal scrollable timeline with scene clips, time ruler, and audio track rows
  - Subtitle editor: font, size, color, background, border, shadow, position controls with live preview
  - Transition picker: 6 transition types in a grid with duration slider
  - Audio track manager: add from library or upload, waveform display, volume/timing controls
  - Waveform: wavesurfer.js renders audio waveforms for each track
  - Audio library dialog: browse and select royalty-free BGM/SFX
  - Video preview: 9:16 Vidstack Player for exported video, scene-by-scene preview with subtitle overlay for pre-export
  - Export button: triggers export job, shows progress bar, download link on completion
  - Voice tab (Tab 3): TTS controls + audio track manager combined
  - Video tab (Tab 4): preview + edit panel (subtitle/transition) + timeline + export
  - Workflow tabs: all 4 tabs enabled and wired with VoiceTab and VideoTab components
  - All tests pass: `npx vitest run` exits 0
  - TypeScript compiles: `npx tsc --noEmit` exits 0
</done_when>
