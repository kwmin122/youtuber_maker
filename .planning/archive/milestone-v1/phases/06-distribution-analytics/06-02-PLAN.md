---
phase: 6
plan: 02
title: Analytics dashboard, upload management UI, SEO preview, thumbnail gallery, viral score display, multi-platform selector
wave: 2
depends_on: [06-01]
files_modified:
  - package.json
  - src/app/(dashboard)/dashboard/analytics/page.tsx
  - src/app/(dashboard)/dashboard/analytics/layout.tsx
  - src/components/analytics/metrics-chart.tsx
  - src/components/analytics/video-performance-table.tsx
  - src/components/analytics/channel-summary.tsx
  - src/components/distribution/upload-panel.tsx
  - src/components/distribution/upload-progress.tsx
  - src/components/distribution/platform-selector.tsx
  - src/components/distribution/seo-preview.tsx
  - src/components/distribution/thumbnail-gallery.tsx
  - src/components/distribution/viral-score-display.tsx
  - src/components/distribution/scheduled-upload-picker.tsx
  - src/components/workflow-tabs.tsx
  - src/hooks/use-job-status.ts
  - src/__tests__/seo-preview.test.tsx
  - src/__tests__/viral-score-display.test.tsx
  - src/__tests__/thumbnail-gallery.test.tsx
  - src/__tests__/platform-selector.test.tsx
requirements_addressed:
  - DATA-01
  - DATA-02
  - MULTI-01
gap_closure: false
---

<objective>
Build the front-end layer for Phase 6: an analytics dashboard page with recharts (line charts for views/likes/comments over time, video performance table), upload management UI with YouTube OAuth flow and scheduled upload date picker, SEO preview/edit UI with inline editing of AI-generated title/description/hashtags, thumbnail gallery with A/B comparison and selection, viral score display with 4-dimension breakdown, and multi-platform upload selector (YouTube=active, TikTok/Reels="Coming Soon"). Wire analytics into the dashboard sidebar navigation.

Requirements fulfilled: DATA-01 (analytics dashboard), DATA-02 (trend keywords shown via SEO hashtags), MULTI-01 (multi-platform selector UI)
Phase goal contribution: Completes the user-facing distribution & analytics experience.
Complexity: L
</objective>

<tasks>
<task id="6-02-01" type="auto">
  <name>Install recharts dependency</name>
  <files>
    - package.json
  </files>
  <read_first>
    - package.json
  </read_first>
  <action>
    Install recharts for dashboard charts:

    ```bash
    npm install recharts
    ```

    This adds `recharts` to `dependencies` in `package.json`.
  </action>
  <acceptance_criteria>
    - `grep -q 'recharts' package.json` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task id="6-02-02" type="auto">
  <name>Build metrics line chart component</name>
  <files>
    - src/components/analytics/metrics-chart.tsx
  </files>
  <read_first>
    - src/lib/distribution/types.ts
  </read_first>
  <action>
    Create `src/components/analytics/metrics-chart.tsx`:

    A "use client" component that renders a recharts `LineChart` with time-series data.

    Props:
    ```typescript
    interface MetricsChartProps {
      data: Array<{
        date: string;
        viewCount: number;
        likeCount: number;
        commentCount: number;
      }>;
      title?: string;
    }
    ```

    Implementation:
    1. Import `LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer` from recharts.
    2. Render a `ResponsiveContainer` with height 300.
    3. Inside, render a `LineChart` with `data` prop.
    4. Three `Line` components:
       - viewCount: blue (#3B82F6), label "Views"
       - likeCount: green (#10B981), label "Likes"
       - commentCount: orange (#F59E0B), label "Comments"
    5. XAxis uses `date` field, formatted as MM/DD.
    6. YAxis with abbreviated number format (1K, 10K, 1M).
    7. Tooltip with formatted values.
    8. Show the title in a `<h3>` above the chart if provided.
    9. Handle empty data: show a "No data yet" placeholder with a chart icon.
  </action>
  <acceptance_criteria>
    - `ls src/components/analytics/metrics-chart.tsx` exits 0
    - `grep -q 'LineChart' src/components/analytics/metrics-chart.tsx` exits 0
    - `grep -q 'viewCount' src/components/analytics/metrics-chart.tsx` exits 0
    - `grep -q 'ResponsiveContainer' src/components/analytics/metrics-chart.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task id="6-02-03" type="auto">
  <name>Build video performance table component</name>
  <files>
    - src/components/analytics/video-performance-table.tsx
  </files>
  <read_first>
    - src/lib/db/schema.ts
  </read_first>
  <action>
    Create `src/components/analytics/video-performance-table.tsx`:

    A "use client" component showing uploaded videos with their performance metrics in a table.

    Props:
    ```typescript
    interface VideoPerformanceTableProps {
      uploads: Array<{
        id: string;
        title: string;
        platform: string;
        youtubeVideoId: string | null;
        videoUrl: string | null;
        status: string;
        uploadedAt: string | null;
        viewCount: number;
        likeCount: number;
        commentCount: number;
      }>;
    }
    ```

    Implementation:
    1. Render an HTML `<table>` styled with Tailwind (border, rounded, hover rows).
    2. Columns: Title, Platform, Status (badge), Views, Likes, Comments, Uploaded At, Link.
    3. Status badges: completed=green, uploading=blue animate-pulse, failed=red, scheduled=yellow.
    4. Platform icons: YouTube logo emoji or text badge.
    5. Link column: external link icon opening `videoUrl` in new tab (only if status=completed).
    6. Sort by uploadedAt desc.
    7. Empty state: "No uploads yet. Export a video and upload it to YouTube."
  </action>
  <acceptance_criteria>
    - `ls src/components/analytics/video-performance-table.tsx` exits 0
    - `grep -q 'VideoPerformanceTable' src/components/analytics/video-performance-table.tsx` exits 0
    - `grep -q 'table' src/components/analytics/video-performance-table.tsx` exits 0
    - `grep -q 'status' src/components/analytics/video-performance-table.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task id="6-02-04" type="auto">
  <name>Build channel summary stats component</name>
  <files>
    - src/components/analytics/channel-summary.tsx
  </files>
  <read_first>
    - src/lib/distribution/types.ts
  </read_first>
  <action>
    Create `src/components/analytics/channel-summary.tsx`:

    A "use client" component showing aggregate channel statistics in a card grid.

    Props:
    ```typescript
    interface ChannelSummaryProps {
      totalUploads: number;
      totalViews: number;
      totalLikes: number;
      totalComments: number;
      avgViralScore: number | null;
    }
    ```

    Implementation:
    1. Render a grid of 5 stat cards (2x3 or 5-column responsive grid).
    2. Each card: icon + label + value.
       - Uploads: Upload icon, count.
       - Views: Eye icon, abbreviated number (1.2K, 3.4M).
       - Likes: ThumbsUp icon, abbreviated number.
       - Comments: MessageCircle icon, abbreviated number.
       - Avg Viral Score: Zap icon, score/100 or "--" if null.
    3. Use lucide-react icons.
    4. Cards have subtle border, rounded corners, hover shadow.
  </action>
  <acceptance_criteria>
    - `ls src/components/analytics/channel-summary.tsx` exits 0
    - `grep -q 'ChannelSummary' src/components/analytics/channel-summary.tsx` exits 0
    - `grep -q 'totalViews' src/components/analytics/channel-summary.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task id="6-02-05" type="auto">
  <name>Build analytics dashboard page</name>
  <files>
    - src/app/(dashboard)/dashboard/analytics/page.tsx
    - src/app/(dashboard)/dashboard/analytics/layout.tsx
  </files>
  <read_first>
    - src/components/analytics/metrics-chart.tsx
    - src/components/analytics/video-performance-table.tsx
    - src/components/analytics/channel-summary.tsx
    - src/app/(dashboard)/dashboard/layout.tsx
  </read_first>
  <action>
    **1. Create `src/app/(dashboard)/dashboard/analytics/layout.tsx`:**

    Simple layout wrapper that passes children through. Metadata title: "Analytics | YouTuber Min".

    **2. Create `src/app/(dashboard)/dashboard/analytics/page.tsx`:**

    A server component that:
    1. Gets the session (redirect to login if unauthenticated).
    2. Queries the database for:
       a. All uploads for this user (for the performance table).
       b. Latest metrics for each upload (for the chart — aggregate by date).
       c. Summary stats (total uploads, total views, total likes, total comments).
    3. Renders:
       - Page title: "Analytics Dashboard"
       - `<ChannelSummary>` with aggregate stats at the top.
       - `<MetricsChart>` with aggregated daily metrics below.
       - `<VideoPerformanceTable>` with all uploads at the bottom.
    4. If no uploads exist, show a friendly empty state with a link to the projects page.
  </action>
  <acceptance_criteria>
    - `ls src/app/(dashboard)/dashboard/analytics/page.tsx` exits 0
    - `ls src/app/(dashboard)/dashboard/analytics/layout.tsx` exits 0
    - `grep -q 'ChannelSummary' src/app/(dashboard)/dashboard/analytics/page.tsx` exits 0
    - `grep -q 'MetricsChart' src/app/(dashboard)/dashboard/analytics/page.tsx` exits 0
    - `grep -q 'VideoPerformanceTable' src/app/(dashboard)/dashboard/analytics/page.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task id="6-02-06" type="auto">
  <name>Build multi-platform upload selector</name>
  <files>
    - src/components/distribution/platform-selector.tsx
    - src/__tests__/platform-selector.test.tsx
  </files>
  <read_first>
    - src/lib/distribution/types.ts
  </read_first>
  <action>
    Create `src/components/distribution/platform-selector.tsx`:

    A "use client" component for selecting upload target platforms.

    Props:
    ```typescript
    interface PlatformSelectorProps {
      selected: UploadPlatform;
      onSelect: (platform: UploadPlatform) => void;
    }
    ```

    Implementation:
    1. Three platform cards in a horizontal row:
       - YouTube: active, clickable, YouTube red color, YouTube icon.
       - TikTok: disabled, "Coming Soon" badge overlay, grayscale, TikTok icon.
       - Instagram Reels: disabled, "Coming Soon" badge overlay, grayscale, Instagram icon.
    2. Selected platform has a highlighted border (ring-2 ring-primary).
    3. Only YouTube is clickable in v1.
    4. "Coming Soon" badge: absolute positioned, small pill badge with text.

    **Create `src/__tests__/platform-selector.test.tsx`:**
    1. Test that YouTube card is clickable and calls onSelect.
    2. Test that TikTok card shows "Coming Soon" and does not call onSelect.
    3. Test that selected platform has the highlighted styling class.
  </action>
  <acceptance_criteria>
    - `ls src/components/distribution/platform-selector.tsx` exits 0
    - `ls src/__tests__/platform-selector.test.tsx` exits 0
    - `grep -q 'Coming Soon' src/components/distribution/platform-selector.tsx` exits 0
    - `grep -q 'YouTube' src/components/distribution/platform-selector.tsx` exits 0
    - `grep -q 'TikTok' src/components/distribution/platform-selector.tsx` exits 0
    - `grep -q 'onSelect' src/components/distribution/platform-selector.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task id="6-02-07" type="auto">
  <name>Build SEO preview/edit component</name>
  <files>
    - src/components/distribution/seo-preview.tsx
    - src/__tests__/seo-preview.test.tsx
  </files>
  <read_first>
    - src/lib/distribution/types.ts
  </read_first>
  <action>
    Create `src/components/distribution/seo-preview.tsx`:

    A "use client" component for previewing and inline-editing AI-generated SEO metadata.

    Props:
    ```typescript
    interface SEOPreviewProps {
      seo: SEOResult | null;
      onUpdate: (updated: Partial<SEOResult>) => void;
      isLoading?: boolean;
    }
    ```

    Implementation:
    1. If `isLoading`, show skeleton placeholders (animated pulse bars).
    2. If `seo` is null and not loading, show "Generate SEO" placeholder with description.
    3. When seo data is available:
       a. **Title section**: editable input field, character counter (X/100), with title variants shown as clickable chips below that auto-fill when clicked.
       b. **Description section**: editable textarea, character counter (X/5000).
       c. **Hashtags section**: tag pills, each with an X to remove, plus an "Add hashtag" input.
       d. **Tags section**: comma-separated editable text field with total character counter (X/500).
    4. All edits call `onUpdate` with the changed field.
    5. Show a "YouTube Preview" mini-card below: simulated YouTube search result appearance with the title, description snippet, and thumbnail placeholder.

    **Create `src/__tests__/seo-preview.test.tsx`:**
    1. Test that title input renders with correct value and calls onUpdate on change.
    2. Test character counter updates when typing.
    3. Test clicking a title variant fills the title input.
    4. Test hashtag removal calls onUpdate.
    5. Test loading state shows skeleton.
  </action>
  <acceptance_criteria>
    - `ls src/components/distribution/seo-preview.tsx` exits 0
    - `ls src/__tests__/seo-preview.test.tsx` exits 0
    - `grep -q 'SEOPreviewProps' src/components/distribution/seo-preview.tsx` exits 0
    - `grep -q 'onUpdate' src/components/distribution/seo-preview.tsx` exits 0
    - `grep -q 'hashtag' src/components/distribution/seo-preview.tsx || grep -q 'Hashtag' src/components/distribution/seo-preview.tsx` exits 0
    - `grep -q 'titleVariants' src/components/distribution/seo-preview.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task id="6-02-08" type="auto">
  <name>Build thumbnail gallery with A/B comparison</name>
  <files>
    - src/components/distribution/thumbnail-gallery.tsx
    - src/__tests__/thumbnail-gallery.test.tsx
  </files>
  <read_first>
    - src/lib/distribution/types.ts
    - src/lib/db/schema.ts
  </read_first>
  <action>
    Create `src/components/distribution/thumbnail-gallery.tsx`:

    A "use client" component showing 2-3 thumbnail variants side-by-side for A/B comparison.

    Props:
    ```typescript
    interface ThumbnailGalleryProps {
      thumbnails: Array<{
        id: string;
        url: string;
        variant: string;
        isSelected: boolean;
        prompt: string;
      }>;
      onSelect: (thumbnailId: string) => void;
      onDelete: (thumbnailId: string) => void;
      isGenerating?: boolean;
    }
    ```

    Implementation:
    1. Grid layout: 2 or 3 columns depending on thumbnail count.
    2. Each thumbnail card:
       - 16:9 aspect ratio image with `object-cover`.
       - Variant label badge (A, B, C) in top-left corner.
       - "Selected" checkmark overlay if `isSelected`.
       - Click to select (calls onSelect).
       - Delete button (trash icon) in top-right corner (calls onDelete with confirmation).
       - Hover: show the prompt text in a tooltip.
    3. Selected card: ring-2 ring-primary, scale-[1.02] subtle highlight.
    4. If `isGenerating`, show skeleton image placeholders with shimmer animation.
    5. Empty state: "Generate thumbnails to see A/B variants here."

    **Create `src/__tests__/thumbnail-gallery.test.tsx`:**
    1. Test that clicking a thumbnail calls onSelect with correct ID.
    2. Test that selected thumbnail shows the checkmark.
    3. Test delete button calls onDelete.
    4. Test generating state shows skeletons.
  </action>
  <acceptance_criteria>
    - `ls src/components/distribution/thumbnail-gallery.tsx` exits 0
    - `ls src/__tests__/thumbnail-gallery.test.tsx` exits 0
    - `grep -q 'ThumbnailGallery' src/components/distribution/thumbnail-gallery.tsx` exits 0
    - `grep -q 'onSelect' src/components/distribution/thumbnail-gallery.tsx` exits 0
    - `grep -q 'isSelected' src/components/distribution/thumbnail-gallery.tsx` exits 0
    - `grep -q 'variant' src/components/distribution/thumbnail-gallery.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task id="6-02-09" type="auto">
  <name>Build viral score display component</name>
  <files>
    - src/components/distribution/viral-score-display.tsx
    - src/__tests__/viral-score-display.test.tsx
  </files>
  <read_first>
    - src/lib/distribution/types.ts
  </read_first>
  <action>
    Create `src/components/distribution/viral-score-display.tsx`:

    A "use client" component showing the viral score with a 4-dimension breakdown.

    Props:
    ```typescript
    interface ViralScoreDisplayProps {
      result: ViralScoreResult | null;
      isLoading?: boolean;
      onGenerate?: () => void;
    }
    ```

    Implementation:
    1. If `result` is null and not loading, show "Analyze Viral Potential" button (calls onGenerate).
    2. If loading, show animated skeleton with circular progress placeholder.
    3. When result available:
       a. **Overall score**: large circular gauge (CSS-based, not a chart library). Score number in center, colored by verdict (viral=green, promising=blue, average=yellow, weak=red). Verdict label below.
       b. **Breakdown bars**: 4 horizontal progress bars, each labeled:
          - Hook Strength (X/25)
          - Emotional Trigger (X/25)
          - Trend Fit (X/25)
          - Title Clickability (X/25)
       c. Bar colors: gradient from red (low) to green (high).
       d. **Suggestions**: bullet list of improvement suggestions below the breakdown.
    4. Use CSS custom properties for the circular gauge (conic-gradient).

    **Create `src/__tests__/viral-score-display.test.tsx`:**
    1. Test that score number renders correctly.
    2. Test that verdict label matches score range.
    3. Test that all 4 breakdown dimensions render.
    4. Test that suggestions list renders.
    5. Test generate button appears when result is null.
  </action>
  <acceptance_criteria>
    - `ls src/components/distribution/viral-score-display.tsx` exits 0
    - `ls src/__tests__/viral-score-display.test.tsx` exits 0
    - `grep -q 'ViralScoreDisplay' src/components/distribution/viral-score-display.tsx` exits 0
    - `grep -q 'hookStrength' src/components/distribution/viral-score-display.tsx` exits 0
    - `grep -q 'emotionalTrigger' src/components/distribution/viral-score-display.tsx` exits 0
    - `grep -q 'verdict' src/components/distribution/viral-score-display.tsx` exits 0
    - `grep -q 'suggestions' src/components/distribution/viral-score-display.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task id="6-02-10" type="auto">
  <name>Build upload panel with scheduled date picker and progress tracking</name>
  <files>
    - src/components/distribution/upload-panel.tsx
    - src/components/distribution/upload-progress.tsx
    - src/components/distribution/scheduled-upload-picker.tsx
  </files>
  <read_first>
    - src/lib/distribution/types.ts
    - src/hooks/use-job-status.ts
    - src/app/api/projects/[id]/upload/route.ts
  </read_first>
  <action>
    **1. Create `src/components/distribution/scheduled-upload-picker.tsx`:**

    A "use client" component for selecting a scheduled publish date/time.

    Props: `{ value: string | null; onChange: (iso: string | null) => void; }`

    Implementation:
    - Date input + time input side by side.
    - "Schedule for later" toggle. Off = immediate upload, On = show date/time pickers.
    - Minimum date: current datetime + 15 minutes.
    - Output: ISO 8601 string or null.
    - Show a note: "Scheduled uploads are set to Private until the publish time."

    **2. Create `src/components/distribution/upload-progress.tsx`:**

    A "use client" component that tracks upload job progress.

    Props: `{ jobId: string | null; }`

    Implementation:
    - Uses `useJobStatus(jobId)` hook (already exists) to poll job progress.
    - Progress bar with percentage.
    - Current step text (e.g., "Uploading...", "Processing...", "Setting thumbnail...").
    - Status badge (pending, uploading, completed, failed).
    - On completion: show link to the uploaded video.
    - On failure: show error message with retry button.

    **3. Create `src/components/distribution/upload-panel.tsx`:**

    A "use client" component that combines all upload-related UI elements.

    Props:
    ```typescript
    interface UploadPanelProps {
      projectId: string;
      hasExportedVideo: boolean;
      seo: SEOResult | null;
      selectedThumbnailId: string | null;
    }
    ```

    Implementation:
    1. If no exported video, show disabled state: "Export your video first before uploading."
    2. Platform selector at the top.
    3. Privacy status radio group (public/unlisted/private).
    4. Scheduled upload picker.
    5. "Upload to YouTube" button — triggers POST to `/api/projects/{id}/upload` with the SEO title/description/tags, privacy status, publishAt, and thumbnailId.
    6. Upload progress component appears after submission.
    7. Upload history section: fetches GET `/api/projects/{id}/upload` and shows past uploads.
  </action>
  <acceptance_criteria>
    - `ls src/components/distribution/upload-panel.tsx` exits 0
    - `ls src/components/distribution/upload-progress.tsx` exits 0
    - `ls src/components/distribution/scheduled-upload-picker.tsx` exits 0
    - `grep -q 'UploadPanel' src/components/distribution/upload-panel.tsx` exits 0
    - `grep -q 'useJobStatus' src/components/distribution/upload-progress.tsx` exits 0
    - `grep -q 'publishAt' src/components/distribution/scheduled-upload-picker.tsx || grep -q 'Schedule' src/components/distribution/scheduled-upload-picker.tsx` exits 0
    - `grep -q 'privacyStatus' src/components/distribution/upload-panel.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task id="6-02-11" type="auto">
  <name>Wire analytics into dashboard sidebar navigation</name>
  <files>
    - src/components/workflow-tabs.tsx
  </files>
  <read_first>
    - src/components/workflow-tabs.tsx
    - src/app/(dashboard)/dashboard/analytics/page.tsx
  </read_first>
  <action>
    Update `src/components/workflow-tabs.tsx` (or the dashboard sidebar/navigation component):

    Add an "Analytics" tab/link to the navigation:
    1. Add a new tab entry with:
       - Label: "Analytics"
       - Icon: BarChart3 from lucide-react
       - Path: `/dashboard/analytics`
    2. Position it after the existing workflow tabs (as a separate section or at the end).
    3. If workflow-tabs.tsx is purely for the 4-step workflow, create the navigation entry in the appropriate sidebar/layout component instead.
    4. Also ensure the distribution-related tabs (Upload, SEO, Thumbnails) are accessible from the project workflow — these may be added as a 5th step or as sub-actions within the final step.
  </action>
  <acceptance_criteria>
    - `grep -q 'analytics' src/components/workflow-tabs.tsx || grep -q 'Analytics' src/components/workflow-tabs.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>
</tasks>

<done_when>
  - recharts installed and importable
  - Analytics dashboard page renders at /dashboard/analytics with channel summary, metrics line chart, and video performance table
  - Metrics chart shows views/likes/comments over time with recharts LineChart
  - Video performance table shows all uploads with status badges and links
  - Upload panel has YouTube OAuth flow trigger, privacy status selector, scheduled date picker, and progress tracking via useJobStatus
  - SEO preview shows AI-generated title/description/hashtags with inline editing, character counters, and title variant chips
  - Thumbnail gallery shows 2-3 variants side-by-side with selection and delete capability
  - Viral score display shows 0-100 circular gauge with 4-dimension breakdown bars and suggestions
  - Multi-platform selector shows YouTube (active) with TikTok/Reels "Coming Soon" badges
  - Analytics link added to dashboard navigation
  - All tests pass: `npx vitest run` exits 0
  - TypeScript compiles: `npx tsc --noEmit` exits 0
</done_when>
