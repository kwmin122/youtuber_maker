---
phase: 1
plan: 01
title: Scaffold app, auth, DB schema, crypto, API keys, project CRUD
wave: 1
depends_on: []
files_modified:
  - package.json
  - drizzle.config.ts
  - src/lib/db/index.ts
  - src/lib/db/schema.ts
  - src/lib/auth.ts
  - src/lib/auth-client.ts
  - src/lib/crypto.ts
  - src/lib/env.ts
  - src/app/api/auth/[...all]/route.ts
  - src/app/api/api-keys/route.ts
  - src/app/api/projects/route.ts
  - src/app/api/projects/[id]/route.ts
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/signup/page.tsx
  - src/app/(dashboard)/layout.tsx
  - src/app/(dashboard)/projects/page.tsx
  - src/app/(dashboard)/projects/[id]/page.tsx
  - src/app/(dashboard)/settings/api-keys/page.tsx
  - src/components/api-key-form.tsx
  - src/components/api-key-list.tsx
  - src/components/project-list.tsx
  - src/hooks/use-session.ts
  - .env.example
  - vitest.config.ts
  - src/__tests__/crypto.test.ts
  - src/__tests__/api-keys.test.ts
  - src/__tests__/projects.test.ts
requirements_addressed:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - UX-02
gap_closure: false
---

<objective>
Clone the nextjs-better-auth fork, extend with custom DB tables (api_keys, projects, jobs, job_events), implement envelope encryption for API key storage, build API key CRUD and project CRUD endpoints with corresponding UI pages, and configure email + Google OAuth login.

Requirements fulfilled: INFRA-01, INFRA-02, INFRA-03, UX-02
Phase goal contribution: Establishes the entire web application foundation — authentication, encrypted API key management, and project persistence — which all subsequent phases build upon.
Complexity: L
</objective>

<tasks>
<task id="1-01-01" type="auto">
  <name>Clone fork and install dependencies</name>
  <files>
    - package.json
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md
    - .planning/phases/01-foundation/01-RESEARCH.md
    - CLAUDE.md
  </read_first>
  <action>
    1. Clone `jabirdev/nextjs-better-auth` (MIT) into the project root, preserving existing `.planning/` and `CLAUDE.md`.
       ```bash
       git clone https://github.com/jabirdev/nextjs-better-auth.git temp-fork
       # Copy fork contents (excluding .git, .planning) into project root
       cp -r temp-fork/* . 
       cp -r temp-fork/.* . 2>/dev/null || true  # hidden files except .git
       rm -rf temp-fork
       ```
    2. Install additional dependencies:
       ```bash
       npm install bullmq ioredis @supabase/supabase-js @t3-oss/env-nextjs zod jose
       npm install -D vitest @vitejs/plugin-react
       ```
    3. Verify `package.json` contains: `next`, `react`, `better-auth`, `drizzle-orm`, `drizzle-kit`, `tailwindcss`, `bullmq`, `ioredis`, `@supabase/supabase-js`, `zod`, `jose`.
  </action>
  <acceptance_criteria>
    - `grep -q '"bullmq"' package.json` exits 0
    - `grep -q '"ioredis"' package.json` exits 0
    - `grep -q '"@supabase/supabase-js"' package.json` exits 0
    - `grep -q '"jose"' package.json` exits 0
    - `grep -q '"vitest"' package.json` exits 0
    - `grep -q '"better-auth"' package.json` exits 0
    - `grep -q '"drizzle-orm"' package.json` exits 0
  </acceptance_criteria>
</task>

<task id="1-01-02" type="auto">
  <name>Create environment variable schema</name>
  <files>
    - src/lib/env.ts
    - .env.example
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md
    - .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
    Create `src/lib/env.ts` using `@t3-oss/env-nextjs` with zod validation:
    ```typescript
    import { createEnv } from '@t3-oss/env-nextjs';
    import { z } from 'zod';

    export const env = createEnv({
      server: {
        DATABASE_URL: z.string().url(),
        BETTER_AUTH_SECRET: z.string().min(32),
        BETTER_AUTH_URL: z.string().url(),
        GOOGLE_CLIENT_ID: z.string().min(1),
        GOOGLE_CLIENT_SECRET: z.string().min(1),
        MASTER_ENCRYPTION_KEY: z.string().min(32),
        REDIS_URL: z.string().url(),
        SUPABASE_JWT_SECRET: z.string().min(32),
      },
      client: {
        NEXT_PUBLIC_SUPABASE_URL: z.string().url(),
        NEXT_PUBLIC_SUPABASE_ANON_KEY: z.string().min(1),
      },
      runtimeEnv: {
        DATABASE_URL: process.env.DATABASE_URL,
        BETTER_AUTH_SECRET: process.env.BETTER_AUTH_SECRET,
        BETTER_AUTH_URL: process.env.BETTER_AUTH_URL,
        GOOGLE_CLIENT_ID: process.env.GOOGLE_CLIENT_ID,
        GOOGLE_CLIENT_SECRET: process.env.GOOGLE_CLIENT_SECRET,
        MASTER_ENCRYPTION_KEY: process.env.MASTER_ENCRYPTION_KEY,
        REDIS_URL: process.env.REDIS_URL,
        SUPABASE_JWT_SECRET: process.env.SUPABASE_JWT_SECRET,
        NEXT_PUBLIC_SUPABASE_URL: process.env.NEXT_PUBLIC_SUPABASE_URL,
        NEXT_PUBLIC_SUPABASE_ANON_KEY: process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,
      },
    });
    ```

    Create `.env.example` with all keys listed (no values), comments per section:
    ```
    # Auth (better-auth)
    BETTER_AUTH_SECRET=
    BETTER_AUTH_URL=http://localhost:3000
    GOOGLE_CLIENT_ID=
    GOOGLE_CLIENT_SECRET=

    # Database (Supabase PostgreSQL)
    DATABASE_URL=

    # Encryption
    MASTER_ENCRYPTION_KEY=

    # Queue (BullMQ)
    REDIS_URL=

    # Supabase (Realtime only - NOT auth)
    NEXT_PUBLIC_SUPABASE_URL=
    NEXT_PUBLIC_SUPABASE_ANON_KEY=
    SUPABASE_JWT_SECRET=
    ```
  </action>
  <acceptance_criteria>
    - `grep -q 'MASTER_ENCRYPTION_KEY' src/lib/env.ts` exits 0
    - `grep -q 'REDIS_URL' src/lib/env.ts` exits 0
    - `grep -q 'SUPABASE_JWT_SECRET' src/lib/env.ts` exits 0
    - `grep -q 'NEXT_PUBLIC_SUPABASE_URL' .env.example` exits 0
    - `grep -qv 'NEXT_PUBLIC_MASTER' .env.example` exits 0 (no NEXT_PUBLIC prefix on secrets)
  </acceptance_criteria>
</task>

<task id="1-01-03" type="auto">
  <name>Define Drizzle schema with 4 custom tables</name>
  <files>
    - src/lib/db/schema.ts
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md
    - .planning/phases/01-foundation/01-RESEARCH.md
    - src/lib/db/schema.ts
  </read_first>
  <action>
    Extend the existing `src/lib/db/schema.ts` (which contains better-auth's user, session, account, verification tables) by adding 4 custom tables. Use the exact schema from RESEARCH.md "Drizzle Schema for Phase 1 Tables" section:

    1. `apiKeys` table (`api_keys`): id (uuid PK), userId (text FK → user.id cascade), provider (text), label (text nullable), last4 (text), keyVersion (integer default 1), encryptedDek (text base64), dekIv (text base64), dekAuthTag (text base64), ciphertext (text base64), dataIv (text base64), dataAuthTag (text base64), revokedAt (timestamp nullable), lastUsedAt (timestamp nullable), createdAt (timestamp defaultNow), updatedAt (timestamp defaultNow).

    2. `projects` table: id (uuid PK), userId (text FK → user.id cascade), title (text), description (text nullable), workflowState (jsonb typed as `{ currentStep: number; lastActiveTab: string; completedSteps: number[]; lastEditedAt: string; draftFlags: Record<string, boolean> }`), createdAt (timestamp), updatedAt (timestamp).

    3. `jobs` table: id (uuid PK), userId (text FK → user.id cascade), projectId (uuid FK → projects.id set null, nullable), type (text), status (text default 'pending'), progress (integer default 0), currentStep (text nullable), errorMessage (text nullable), payload (jsonb), result (jsonb), createdAt (timestamp), updatedAt (timestamp).

    4. `jobEvents` table (`job_events`): id (uuid PK), jobId (uuid FK → jobs.id cascade), event (text), data (jsonb), createdAt (timestamp defaultNow).

    Import from `drizzle-orm/pg-core`: `pgTable`, `text`, `timestamp`, `integer`, `jsonb`, `uuid`.
  </action>
  <acceptance_criteria>
    - `grep -q "api_keys" src/lib/db/schema.ts` exits 0
    - `grep -q "projects" src/lib/db/schema.ts` exits 0
    - `grep -q "jobs" src/lib/db/schema.ts` exits 0
    - `grep -q "job_events" src/lib/db/schema.ts` exits 0
    - `grep -q "encrypted_dek" src/lib/db/schema.ts` exits 0
    - `grep -q "workflow_state" src/lib/db/schema.ts` exits 0
    - `grep -q "key_version" src/lib/db/schema.ts` exits 0
    - `grep -q "user_id" src/lib/db/schema.ts` exits 0
  </acceptance_criteria>
</task>

<task id="1-01-04" type="auto">
  <name>Implement envelope encryption module</name>
  <files>
    - src/lib/crypto.ts
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md
    - .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
    Create `src/lib/crypto.ts` implementing envelope encryption with AES-256-GCM. Use the exact pattern from RESEARCH.md Pattern 1:

    ```typescript
    import { randomBytes, createCipheriv, createDecipheriv } from 'crypto';

    const ALGORITHM = 'aes-256-gcm';
    const IV_LENGTH = 12;
    const KEY_LENGTH = 32;

    export interface EncryptedPayload {
      keyVersion: number;
      encryptedDek: string;  // base64
      dekIv: string;         // base64
      dekAuthTag: string;    // base64
      ciphertext: string;    // base64
      dataIv: string;        // base64
      dataAuthTag: string;   // base64
    }

    export function getMasterKey(): Buffer {
      const key = process.env.MASTER_ENCRYPTION_KEY;
      if (!key || key.length < 32) {
        throw new Error('MASTER_ENCRYPTION_KEY must be at least 32 bytes');
      }
      return Buffer.from(key, 'base64');
    }

    export function encrypt(plaintext: string, masterKey: Buffer): EncryptedPayload {
      const dek = randomBytes(KEY_LENGTH);
      const dataIv = randomBytes(IV_LENGTH);
      const dataCipher = createCipheriv(ALGORITHM, dek, dataIv);
      const ciphertext = Buffer.concat([dataCipher.update(plaintext, 'utf8'), dataCipher.final()]);
      const dataAuthTag = dataCipher.getAuthTag();
      const dekIv = randomBytes(IV_LENGTH);
      const dekCipher = createCipheriv(ALGORITHM, masterKey, dekIv);
      const encryptedDek = Buffer.concat([dekCipher.update(dek), dekCipher.final()]);
      const dekAuthTag = dekCipher.getAuthTag();
      dek.fill(0);
      return {
        keyVersion: 1,
        encryptedDek: encryptedDek.toString('base64'),
        dekIv: dekIv.toString('base64'),
        dekAuthTag: dekAuthTag.toString('base64'),
        ciphertext: ciphertext.toString('base64'),
        dataIv: dataIv.toString('base64'),
        dataAuthTag: dataAuthTag.toString('base64'),
      };
    }

    export function decrypt(payload: EncryptedPayload, masterKey: Buffer): string {
      const dekDecipher = createDecipheriv(ALGORITHM, masterKey, Buffer.from(payload.dekIv, 'base64'));
      dekDecipher.setAuthTag(Buffer.from(payload.dekAuthTag, 'base64'));
      const dek = Buffer.concat([dekDecipher.update(Buffer.from(payload.encryptedDek, 'base64')), dekDecipher.final()]);
      const dataDecipher = createDecipheriv(ALGORITHM, dek, Buffer.from(payload.dataIv, 'base64'));
      dataDecipher.setAuthTag(Buffer.from(payload.dataAuthTag, 'base64'));
      const plaintext = Buffer.concat([dataDecipher.update(Buffer.from(payload.ciphertext, 'base64')), dataDecipher.final()]);
      dek.fill(0);
      return plaintext.toString('utf8');
    }

    export function extractLast4(apiKey: string): string {
      return apiKey.slice(-4);
    }
    ```
  </action>
  <acceptance_criteria>
    - `grep -q "aes-256-gcm" src/lib/crypto.ts` exits 0
    - `grep -q "encrypt" src/lib/crypto.ts` exits 0
    - `grep -q "decrypt" src/lib/crypto.ts` exits 0
    - `grep -q "getMasterKey" src/lib/crypto.ts` exits 0
    - `grep -q "extractLast4" src/lib/crypto.ts` exits 0
    - `grep -q "dek.fill(0)" src/lib/crypto.ts` exits 0
    - `grep -q "EncryptedPayload" src/lib/crypto.ts` exits 0
  </acceptance_criteria>
</task>

<task id="1-01-05" type="auto">
  <name>Configure better-auth with email + Google OAuth</name>
  <files>
    - src/lib/auth.ts
    - src/lib/auth-client.ts
    - src/app/api/auth/[...all]/route.ts
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md
    - .planning/phases/01-foundation/01-RESEARCH.md
    - src/lib/auth.ts
    - src/lib/auth-client.ts
  </read_first>
  <action>
    Update `src/lib/auth.ts` (server config) to include:
    - `emailAndPassword: { enabled: true }` in the `auth` config
    - Google social provider with `clientId: process.env.GOOGLE_CLIENT_ID` and `clientSecret: process.env.GOOGLE_CLIENT_SECRET`
    - Drizzle adapter pointing to the existing db instance
    - `basePath: '/api/auth'`
    - Session expiry: 7 days (`session: { expiresIn: 60 * 60 * 24 * 7 }`)

    Update `src/lib/auth-client.ts` (client config) to include:
    - `baseURL: process.env.NEXT_PUBLIC_BETTER_AUTH_URL || ''`
    - Export `signIn`, `signUp`, `signOut`, `useSession` from the client

    Ensure `src/app/api/auth/[...all]/route.ts` exports GET and POST handlers from the auth instance.
  </action>
  <acceptance_criteria>
    - `grep -q "emailAndPassword" src/lib/auth.ts` exits 0
    - `grep -q "GOOGLE_CLIENT_ID" src/lib/auth.ts` exits 0
    - `grep -q "socialProviders" src/lib/auth.ts` exits 0 OR `grep -q "google" src/lib/auth.ts` exits 0
    - `grep -q "signIn" src/lib/auth-client.ts` exits 0
    - `grep -q "signUp" src/lib/auth-client.ts` exits 0
    - File exists: `src/app/api/auth/[...all]/route.ts`
  </acceptance_criteria>
</task>

<task id="1-01-06" type="auto">
  <name>Build API key CRUD endpoints</name>
  <files>
    - src/app/api/api-keys/route.ts
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md
    - src/lib/crypto.ts
    - src/lib/db/schema.ts
  </read_first>
  <action>
    Create `src/app/api/api-keys/route.ts` with:

    **POST** — Register a new API key:
    - Validate request body with zod: `{ provider: string, label?: string, apiKey: string }`
    - Get authenticated user from better-auth session (reject 401 if no session)
    - Call `encrypt(apiKey, getMasterKey())` to get `EncryptedPayload`
    - Call `extractLast4(apiKey)` to get last 4 chars
    - Insert into `apiKeys` table: userId, provider, label, last4, keyVersion, encryptedDek, dekIv, dekAuthTag, ciphertext, dataIv, dataAuthTag
    - Return `{ id, provider, label, last4, createdAt }` (never return ciphertext or encrypted fields)

    **GET** — List user's API keys:
    - Get authenticated user
    - Query `apiKeys` where `userId = user.id` and `revokedAt IS NULL`
    - Return array of `{ id, provider, label, last4, createdAt, lastUsedAt }`
    - NEVER include encryption fields in response

    **DELETE** — Revoke an API key:
    - Get authenticated user
    - Accept `{ id: string }` in request body
    - Verify key belongs to user
    - Set `revokedAt = new Date()` (soft delete)
    - Return `{ success: true }`

    Import auth session helper from better-auth. Use `headers()` from `next/headers` for auth extraction.
  </action>
  <acceptance_criteria>
    - `grep -q "encrypt" src/app/api/api-keys/route.ts` exits 0
    - `grep -q "getMasterKey" src/app/api/api-keys/route.ts` exits 0
    - `grep -q "extractLast4" src/app/api/api-keys/route.ts` exits 0
    - `grep -q "revokedAt" src/app/api/api-keys/route.ts` exits 0
    - `grep -q "last4" src/app/api/api-keys/route.ts` exits 0
    - `grep -qv "plaintext" src/app/api/api-keys/route.ts` exits 0 (no plaintext in responses)
  </acceptance_criteria>
</task>

<task id="1-01-07" type="auto">
  <name>Build project CRUD endpoints</name>
  <files>
    - src/app/api/projects/route.ts
    - src/app/api/projects/[id]/route.ts
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md
    - src/lib/db/schema.ts
  </read_first>
  <action>
    Create `src/app/api/projects/route.ts`:

    **POST** — Create project:
    - Validate: `{ title: string, description?: string }`
    - Auth guard (401 if no session)
    - Insert into `projects` with userId, title, description, workflowState as `{ currentStep: 1, lastActiveTab: 'script', completedSteps: [], lastEditedAt: new Date().toISOString(), draftFlags: {} }`
    - Return created project

    **GET** — List user's projects:
    - Auth guard
    - Query `projects` where `userId = user.id` ordered by `updatedAt DESC`
    - Return array of projects

    Create `src/app/api/projects/[id]/route.ts`:

    **GET** — Get single project:
    - Auth guard + verify ownership
    - Return project by id

    **PATCH** — Update project:
    - Auth guard + verify ownership
    - Validate: `{ title?: string, description?: string, workflowState?: object }`
    - Update `updatedAt` on every PATCH
    - Return updated project

    **DELETE** — Delete project:
    - Auth guard + verify ownership
    - Delete row from projects
    - Return `{ success: true }`
  </action>
  <acceptance_criteria>
    - `grep -q "projects" src/app/api/projects/route.ts` exits 0
    - `grep -q "workflowState" src/app/api/projects/route.ts` exits 0
    - `grep -q "PATCH" src/app/api/projects/[id]/route.ts` exits 0
    - `grep -q "DELETE" src/app/api/projects/[id]/route.ts` exits 0
    - File exists: `src/app/api/projects/[id]/route.ts`
  </acceptance_criteria>
</task>

<task id="1-01-08" type="auto">
  <name>Build auth UI pages (login + signup)</name>
  <files>
    - src/app/(auth)/login/page.tsx
    - src/app/(auth)/signup/page.tsx
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md
    - src/lib/auth-client.ts
  </read_first>
  <action>
    Create login page `src/app/(auth)/login/page.tsx`:
    - 'use client' directive
    - Email + password form using shadcn/ui Input, Button, Card components
    - Google OAuth login button calling `signIn.social({ provider: 'google' })`
    - Link to signup page
    - On success, redirect to `/projects`
    - Error display for invalid credentials

    Create signup page `src/app/(auth)/signup/page.tsx`:
    - 'use client' directive
    - Email + password + name form
    - Google OAuth signup button
    - Link to login page
    - On success, redirect to `/projects`
    - Validation errors display
  </action>
  <acceptance_criteria>
    - `grep -q "signIn" src/app/\(auth\)/login/page.tsx` exits 0
    - `grep -q "google" src/app/\(auth\)/login/page.tsx` exits 0
    - `grep -q "signUp" src/app/\(auth\)/signup/page.tsx` exits 0
    - `grep -q "'use client'" src/app/\(auth\)/login/page.tsx` exits 0
    - File exists: `src/app/(auth)/login/page.tsx`
    - File exists: `src/app/(auth)/signup/page.tsx`
  </acceptance_criteria>
</task>

<task id="1-01-09" type="auto">
  <name>Build dashboard layout with auth guard</name>
  <files>
    - src/app/(dashboard)/layout.tsx
    - src/hooks/use-session.ts
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md
    - src/lib/auth-client.ts
  </read_first>
  <action>
    Create `src/hooks/use-session.ts`:
    - Export a `useSession` hook wrapping better-auth's `useSession` from auth-client
    - Return `{ user, session, isLoading, error }`

    Create `src/app/(dashboard)/layout.tsx`:
    - Server component or client component with auth check
    - If not authenticated, redirect to `/login`
    - Render sidebar/nav with links: Projects, Settings > API Keys
    - Show user name/email in header
    - Render `{children}` in main content area
  </action>
  <acceptance_criteria>
    - `grep -q "useSession" src/hooks/use-session.ts` exits 0
    - `grep -q "redirect\|router.push" src/app/\(dashboard\)/layout.tsx` exits 0
    - `grep -q "login" src/app/\(dashboard\)/layout.tsx` exits 0
    - File exists: `src/app/(dashboard)/layout.tsx`
  </acceptance_criteria>
</task>

<task id="1-01-10" type="auto">
  <name>Build API key management UI</name>
  <files>
    - src/app/(dashboard)/settings/api-keys/page.tsx
    - src/components/api-key-form.tsx
    - src/components/api-key-list.tsx
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md
    - src/app/api/api-keys/route.ts
  </read_first>
  <action>
    Create `src/components/api-key-form.tsx`:
    - 'use client'
    - Form with fields: provider (select: gemini, openai, kling, custom), label (text, optional), apiKey (password input)
    - Submit calls POST `/api/api-keys`
    - On success, clear form and call onSuccess callback
    - apiKey field uses `type="password"` — never shows plaintext after submission

    Create `src/components/api-key-list.tsx`:
    - 'use client'
    - Fetch GET `/api/api-keys` on mount
    - Display table: provider icon/name, label, `····${last4}` (masked), createdAt, lastUsedAt
    - Delete button calls DELETE `/api/api-keys` with confirmation dialog
    - Never display encrypted fields or full key

    Create `src/app/(dashboard)/settings/api-keys/page.tsx`:
    - Compose `ApiKeyForm` + `ApiKeyList`
    - Title: "API 키 관리"
    - Description text explaining BYOK model
  </action>
  <acceptance_criteria>
    - `grep -q "password" src/components/api-key-form.tsx` exits 0
    - `grep -q "last4" src/components/api-key-list.tsx` exits 0
    - `grep -q "provider" src/components/api-key-list.tsx` exits 0
    - `grep -q "ApiKeyForm" src/app/\(dashboard\)/settings/api-keys/page.tsx` exits 0
    - `grep -q "ApiKeyList" src/app/\(dashboard\)/settings/api-keys/page.tsx` exits 0
  </acceptance_criteria>
</task>

<task id="1-01-11" type="auto">
  <name>Build project list and detail UI</name>
  <files>
    - src/app/(dashboard)/projects/page.tsx
    - src/app/(dashboard)/projects/[id]/page.tsx
    - src/components/project-list.tsx
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md
    - src/app/api/projects/route.ts
    - src/app/api/projects/[id]/route.ts
  </read_first>
  <action>
    Create `src/components/project-list.tsx`:
    - 'use client'
    - Fetch GET `/api/projects` on mount
    - Display card grid: title, description (truncated), updatedAt, workflowState.currentStep
    - "새 프로젝트" button opens create dialog (title + description form, POST `/api/projects`)
    - Click card navigates to `/projects/[id]`

    Create `src/app/(dashboard)/projects/page.tsx`:
    - Render `ProjectList` component
    - Title: "프로젝트"

    Create `src/app/(dashboard)/projects/[id]/page.tsx`:
    - Fetch GET `/api/projects/[id]`
    - Display project title (editable), description (editable)
    - Save button calls PATCH `/api/projects/[id]`
    - Delete button with confirmation calls DELETE `/api/projects/[id]`, redirects to `/projects`
    - Show workflowState.currentStep as progress indicator (placeholder for future tabs)
  </action>
  <acceptance_criteria>
    - `grep -q "ProjectList\|project-list" src/app/\(dashboard\)/projects/page.tsx` exits 0
    - `grep -q "PATCH\|fetch" src/app/\(dashboard\)/projects/\[id\]/page.tsx` exits 0
    - `grep -q "프로젝트\|project" src/components/project-list.tsx` exits 0
    - File exists: `src/app/(dashboard)/projects/[id]/page.tsx`
  </acceptance_criteria>
</task>

<task id="1-01-12" type="auto">
  <name>Create Vitest config and unit tests for crypto</name>
  <files>
    - vitest.config.ts
    - src/__tests__/crypto.test.ts
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md
    - src/lib/crypto.ts
  </read_first>
  <action>
    Create `vitest.config.ts`:
    ```typescript
    import { defineConfig } from 'vitest/config';
    import react from '@vitejs/plugin-react';

    export default defineConfig({
      plugins: [react()],
      test: {
        environment: 'node',
        globals: true,
        include: ['src/**/*.test.ts', 'src/**/*.test.tsx'],
      },
      resolve: {
        alias: {
          '@': './src',
        },
      },
    });
    ```

    Create `src/__tests__/crypto.test.ts`:
    - Test: encrypt then decrypt returns original plaintext
    - Test: different plaintext produces different ciphertext
    - Test: tampering with ciphertext causes decrypt to throw
    - Test: extractLast4 returns correct last 4 characters
    - Test: getMasterKey throws if env var missing
    - Test: DEK is zeroed after encryption (verify dek.fill(0) behavior)
    - Use `randomBytes(32).toString('base64')` as test master key
  </action>
  <acceptance_criteria>
    - `grep -q "vitest" vitest.config.ts` exits 0
    - `grep -q "encrypt" src/__tests__/crypto.test.ts` exits 0
    - `grep -q "decrypt" src/__tests__/crypto.test.ts` exits 0
    - `grep -q "extractLast4" src/__tests__/crypto.test.ts` exits 0
    - `grep -q "describe\|it\|test" src/__tests__/crypto.test.ts` exits 0
  </acceptance_criteria>
</task>

<task id="1-01-13" type="auto">
  <name>Push DB schema with Drizzle Kit</name>
  <files>
    - drizzle.config.ts
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md
    - src/lib/db/schema.ts
    - drizzle.config.ts
  </read_first>
  <action>
    Verify/update `drizzle.config.ts` to point to Supabase PostgreSQL:
    ```typescript
    import { defineConfig } from 'drizzle-kit';

    export default defineConfig({
      schema: './src/lib/db/schema.ts',
      out: './drizzle',
      dialect: 'postgresql',
      dbCredentials: {
        url: process.env.DATABASE_URL!,
      },
    });
    ```

    Add npm scripts to `package.json`:
    - `"db:push": "drizzle-kit push"`
    - `"db:generate": "drizzle-kit generate"`
    - `"db:migrate": "drizzle-kit migrate"`
    - `"db:studio": "drizzle-kit studio"`

    Use `drizzle-kit push` for development (as per RESEARCH.md recommendation).
  </action>
  <acceptance_criteria>
    - `grep -q "schema" drizzle.config.ts` exits 0
    - `grep -q "postgresql" drizzle.config.ts` exits 0
    - `grep -q "db:push" package.json` exits 0
  </acceptance_criteria>
</task>
<task id="1-01-14" type="auto">
  <name>Create unit tests for API key and project endpoints</name>
  <files>
    - src/__tests__/api-keys.test.ts
    - src/__tests__/projects.test.ts
  </files>
  <read_first>
    - src/app/api/api-keys/route.ts
    - src/app/api/projects/route.ts
    - src/app/api/projects/[id]/route.ts
    - src/lib/db/schema.ts
    - vitest.config.ts
  </read_first>
  <action>
    Create `src/__tests__/api-keys.test.ts`:
    - Test: POST /api/api-keys creates an encrypted key and returns only masked fields (id, provider, label, last4, createdAt)
    - Test: GET /api/api-keys lists keys without encryption fields
    - Test: DELETE /api/api-keys soft-deletes by setting revokedAt
    - Test: unauthenticated requests return 401
    - Test: response never contains ciphertext, encryptedDek, or plaintext API key
    - Mock the DB connection and auth session using Vitest mocks

    Create `src/__tests__/projects.test.ts`:
    - Test: POST /api/projects creates a project with default workflowState
    - Test: GET /api/projects lists only the authenticated user's projects
    - Test: GET /api/projects/[id] returns a single project with ownership check
    - Test: PATCH /api/projects/[id] updates title, description, workflowState and bumps updatedAt
    - Test: DELETE /api/projects/[id] removes the project
    - Test: unauthenticated requests return 401
    - Mock the DB connection and auth session using Vitest mocks
  </action>
  <acceptance_criteria>
    - `grep -q "api-keys\|apiKeys" src/__tests__/api-keys.test.ts` exits 0
    - `grep -q "last4" src/__tests__/api-keys.test.ts` exits 0
    - `grep -q "401" src/__tests__/api-keys.test.ts` exits 0
    - `grep -q "describe\|it\|test" src/__tests__/api-keys.test.ts` exits 0
    - `grep -q "projects" src/__tests__/projects.test.ts` exits 0
    - `grep -q "workflowState" src/__tests__/projects.test.ts` exits 0
    - `grep -q "PATCH\|update" src/__tests__/projects.test.ts` exits 0
    - `grep -q "describe\|it\|test" src/__tests__/projects.test.ts` exits 0
  </acceptance_criteria>
</task>

<task id="1-01-15" type="auto">
  <name>Submit Google OAuth verification and YouTube API quota increase requests</name>
  <files>
    - .planning/phases/01-foundation/oauth-verification-checklist.md
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
    This task addresses CONTEXT.md Decision D-03 and ROADMAP.md Phase 1 Success Criteria items 5-6.

    1. **Google OAuth 인증 심사 신청:**
       - Google Cloud Console에서 OAuth consent screen 설정 완료
       - YouTube Data API v3 활성화
       - 필요 scope 등록: `https://www.googleapis.com/auth/youtube.upload`, `https://www.googleapis.com/auth/youtube`
       - OAuth 브랜드 인증 심사 제출 (4-8주 소요이므로 Phase 1에서 선행 필수)
       - 앱 이름, 로고, 개인정보처리방침 URL, 이용약관 URL 준비
       - 심사 상태 추적용 체크리스트 파일 생성: `.planning/phases/01-foundation/oauth-verification-checklist.md`

    2. **YouTube API 쿼터 증량 신청:**
       - Google Cloud Console → APIs & Services → YouTube Data API v3 → Quotas
       - 기본 쿼터 (10,000 units/day) → 증량 신청 양식 작성
       - 사용 사례 설명: "AI-automated YouTube Shorts creation and upload SaaS"
       - 예상 일일 사용량 산정 (upload = 1,600 units/call 기준)

    3. **체크리스트 파일 생성** (`oauth-verification-checklist.md`):
       ```markdown
       # Google OAuth Verification Checklist

       ## OAuth Consent Screen
       - [ ] App name configured
       - [ ] Support email set
       - [ ] App logo uploaded
       - [ ] Privacy policy URL added
       - [ ] Terms of service URL added

       ## Scopes Registered
       - [ ] youtube.upload scope added
       - [ ] youtube scope added

       ## Verification Submission
       - [ ] OAuth verification submitted to Google
       - [ ] Submission date recorded: ____
       - [ ] Expected approval: ____ (4-8 weeks)

       ## YouTube API Quota
       - [ ] Quota increase request submitted
       - [ ] Submission date recorded: ____
       - [ ] Requested quota amount: ____
       ```

    Note: The actual submission to Google Console is a manual step. This task creates the tracking checklist and documents the required actions.
  </action>
  <acceptance_criteria>
    - File exists: `.planning/phases/01-foundation/oauth-verification-checklist.md`
    - `grep -q "youtube.upload" .planning/phases/01-foundation/oauth-verification-checklist.md` exits 0
    - `grep -q "OAuth verification" .planning/phases/01-foundation/oauth-verification-checklist.md` exits 0
    - `grep -q "Quota" .planning/phases/01-foundation/oauth-verification-checklist.md` exits 0
  </acceptance_criteria>
</task>
</tasks>

<done_when>
- [ ] Fork cloned, dependencies installed, `npm run build` or `npx next build` succeeds
- [ ] Email + Google OAuth login functional (better-auth configured)
- [ ] API key registration encrypts with AES-256-GCM, returns only masked fields (last4)
- [ ] API key list shows provider, label, last4, createdAt — never encryption fields
- [ ] Project CRUD (create, list, get, update, delete) works end-to-end
- [ ] Crypto unit tests pass: `npx vitest run src/__tests__/crypto.test.ts`
- [ ] DB schema has 4 custom tables + better-auth tables
- [ ] API key endpoint tests pass: `npx vitest run src/__tests__/api-keys.test.ts`
- [ ] Project endpoint tests pass: `npx vitest run src/__tests__/projects.test.ts`
- [ ] Google OAuth 인증 심사 신청 완료 (체크리스트 기록)
- [ ] YouTube API 쿼터 증량 신청 완료 (체크리스트 기록)
- [ ] All task acceptance criteria verified
- [ ] /sunco:lint passes with zero errors
- [ ] npx tsc --noEmit passes with zero errors
</done_when>
